# Optional models

The required classical pipeline does not need trained weights. Store only small,
redistributable optional model artifacts or adapter metadata here. Document the
source, version, license, checksum, loading code, and fallback behavior for every
artifact. Never make the grammar/parser/semantic baseline depend on this folder.
